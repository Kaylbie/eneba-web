generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Game{
  id Int @id @default(autoincrement())
  gameName String
  title String
  imageSrc String
  region String
  price Decimal
  discount Int
  likes Int
  isAvailable Boolean
  platform String
}

